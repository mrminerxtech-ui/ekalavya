name: Notify Railway Backend Deploy

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Backend deployed
        run: echo "Railway auto-deploys from GitHub. Backend updated."
